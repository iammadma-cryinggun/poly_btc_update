# 如何删除 master 分支

## ⚠️ 重要提示

GitHub 的 **master** 分支是默认分支，无法用命令行删除！

**必须先在 GitHub 网页上把默认分支改为 main！**

---

## 🎯 方法 1：网页操作（3步搞定）

### 第 1 步：改默认分支

1. **打开分支设置页面**
   ```
   https://github.com/iammadma-cryinggun/poly_btc_update/settings/branches
   ```

2. **更改默认分支**
   - 找到 "Default branch"
   - 点击 🔄 图标
   - 选择 "main"
   - 点击 "Update"
   - 输入 `poly_btc_update` 确认

### 第 2 步：删除 master 分支

1. **在同一页面**
   - 向下滚动到 "Branches" 部分
   - 找到 "master" 分支
   - 点击右侧的 "..."
   - 选择 "Delete branch"
   - 确认删除

### 第 3 步：验证

```bash
git fetch origin
git branch -r
```

应该只看到：
```
origin/main
```

---

## ✅ 完成后

仓库就只有一个 **main** 分支了！

然后就可以在 Zeabur 部署，不会混淆了。
