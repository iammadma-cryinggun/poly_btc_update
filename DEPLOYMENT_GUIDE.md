# 完整部署指南 - Polymarket 做市策略

**目标**: 从零开始部署一个完整的 Polymarket 做市交易系统

---

## 📋 部署检查清单

### ✅ 准备工作

- [ ] Python 3.10+ 已安装
- [ ] Polymarket 邮箱账号已注册
- [ ] 钱包已充值 5-10 USDC
- [ ] （可选）GitHub 账号（用于 Zeabur 部署）

---

## 🚀 步骤 1: 获取市场信息（必须）

### 为什么需要这一步？

Polymarket 每天都有新的 BTC 日内市场，每个市场都有唯一的 token ID。**必须先获取正确的 token，否则策略无法交易。**

### 方法 A: 使用快速配置工具（推荐）

```bash
cd D:\翻倍项目\polymarket_v2
python quick_setup.py
```

选择 **选项 3** - 使用测试市场（后续可以更新）

### 方法 B: 从浏览器手动获取

1. **访问市场页面**
   ```
   https://polymarket.com/event/bitcoin-up-or-down-on-january-29
   ```

2. **打开浏览器开发者工具**
   - Windows: `F12`
   - Mac: `Cmd + Option + I`

3. **切换到 Console 标签**

4. **运行以下代码**
   ```javascript
   const m = __INITIAL_STATE__.markets.activeMarkets[0];
   copy({
     slug: m.slug,
     condition_id: m.conditionId,
     token_id: m.clobTokenIds[0],
     question: m.question
   });
   console.log('✅ 已复制到剪贴板!');
   ```

5. **运行配置工具**
   ```bash
   python market_config_manager.py --manual
   ```

6. **粘贴信息**

### 验证配置

```bash
python market_config_manager.py --show
```

应该看到：
```
Slug: bitcoin-up-or-down-on-january-29
Condition ID: 0x...
Token ID: 长字符串...
```

---

## 🔐 步骤 2: 配置 API 凭证

### 生成 API 凭证

```bash
python generate_api_credentials.py
```

这会：
1. 读取 `.env` 文件中的私钥
2. 连接到 Polymarket
3. 生成/获取 API 凭证
4. 保存到 `.env` 文件

### 验证 .env 文件

```bash
type .env
```

应该包含：
```env
POLYMARKET_PK=0x...
POLYMARKET_API_KEY=xxx-xxx-xxx
POLYMARKET_API_SECRET=...
POLYMARKET_PASSPHRASE=...
```

---

## 🎯 步骤 3: 测试运行

### 使用主启动脚本

```bash
python start.py
```

选择 **选项 1** - 启动策略

### 预期输出

```
================================================================================
Polymarket 做市策略 - 小资金安全测试
================================================================================

[检查 1/3] 环境变量
  ✅ 环境变量配置正确

[检查 2/3] 市场配置
  ✅ 市场配置: bitcoin-up-or-down-on-january-29
     URL: https://polymarket.com/event/bitcoin-up-or-down-on-january-29

[检查 3/3] 策略文件
  ✅ 所有策略文件存在

✅ 所有检查通过，启动策略...
```

### 观察运行

策略启动后，您应该看到：
```
[INFO] TradingNode 初始化成功
[INFO] MarketMakingStrategy: READY
[OK] 订单已提交
做市参数:
  中间价: 0.5000
  价差: 5.00%
  买价: 0.4750
  卖价: 0.5250
```

### 停止策略

按 `Ctrl+C`

---

## 📅 步骤 4: 每天更新市场

### 自动滚动（推荐）

```bash
python market_config_manager.py --next-day
```

工具会：
1. 显示当前市场
2. 生成下一天的市场 URL
3. 提示您输入新市场的 token
4. 更新配置文件

### 重新启动

```bash
python start.py
```

选择 **选项 1**

---

## 🌐 步骤 5: 部署到 Zeabur（可选但推荐）

### 为什么部署到 Zeabur？

- ✅ 24/7 运行
- ✅ 网络稳定（美国服务器）
- ✅ 免费额度充足
- ✅ 实时日志查看

### 部署步骤

#### 5.1 推送代码到 GitHub

```bash
cd D:\翻倍项目\polymarket_v2

# 添加所有文件
git add .

# 提交
git commit -m "部署 Polymarket 做市策略"

# 推送
git push origin main
```

#### 5.2 在 Zeabur 创建项目

1. 访问 https://zeabur.com
2. 使用 GitHub 登录
3. 点击 **"New Project"**
4. 选择 **"Deploy from GitHub"**
5. 选择 `poly_btc_update` 仓库

#### 5.3 配置服务

**启动命令**:
```bash
python start.py
```

或者直接运行策略：
```bash
python run_market_making_safe.py
```

#### 5.4 设置环境变量

在 Zeabur 项目设置中添加：

```bash
POLYMARKET_PK=0xed298792ddcc1e6348a934214fa563324800bfa72ac58fc066ea4ba8a17b1feb
POLYMARKET_API_KEY=e66cd14b-997d-2da4-e2f6-a5c1d411d23a
POLYMARKET_API_SECRET=Hl_DkIvovhSyzOFXamQICLJFpQIkBpSXDMGLQZCb3_c=
POLYMARKET_PASSPHRASE=d29d194faaf7dac6d3dc5f3165b9251117945f43d60261c8b833494afbe4c1c4
```

**重要**: 在 Zeabur 中，您还需要创建 `market_config.json` 文件或通过环境变量设置。

#### 5.5 启动服务

点击 **"Deploy"** 按钮

等待 2-3 分钟构建完成

#### 5.6 监控日志

在 Zeabur 控制台：
1. 点击您的服务
2. 选择 **"Logs"** 标签
3. 实时查看策略运行

---

## 📊 监控和管理

### 本地运行监控

```bash
# 查看状态
python start.py

# 选择选项 4 - 查看状态
```

### Zeabur 监控

- **Logs**: 实时日志
- **Metrics**: CPU/内存使用
- **Restart**: 重启服务

### 关键指标

| 指标 | 健康状态 |
|------|---------|
| 订单提交 | ✅ 每分钟都有订单 |
| 成交率 | ✅ 10-30% |
| 库存 | ✅ < 5 个 token |
| 日盈亏 | ✅ > -1 USDC |

---

## 🔄 日常运维

### 每天早上

1. **检查日志**
   - 确认策略正常运行
   - 查看昨日盈亏

2. **更新市场**（如需要）
   ```bash
   python market_config_manager.py --next-day
   ```

3. **重启策略**
   - 本地: `python start.py`
   - Zeabur: 在控制台点击 Restart

### 每周

1. **检查总盈亏**
2. **优化参数**（如需要）
3. **更新代码**（如有新版本）

---

## 🐛 故障排除

### 问题 1: 策略无法启动

**症状**: 配置检查失败

**解决方案**:
```bash
# 检查状态
python start.py
# 选择选项 4

# 重新生成 API 凭证
python generate_api_credentials.py
```

### 问题 2: 没有订单成交

**症状**: 日志显示订单提交但无成交

**可能原因**:
1. 市场已到期
2. 价差过大
3. 流动性不足

**解决方案**:
```bash
# 1. 更新市场
python market_config_manager.py --next-day

# 2. 检查市场是否活跃
# 访问 Polymarket 网页确认

# 3. 降低价差（在 run_market_making_safe.py 中修改）
```

### 问题 3: 网络连接失败

**症状**: `Request exception` 或 `Connection timeout`

**解决方案**:
- 检查网络连接
- 使用 Zeabur 部署（网络更稳定）
- 尝试使用 VPN

### 问题 4: API 错误

**症状**: `PolyApiException`

**解决方案**:
```bash
# 重新生成 API 凭证
python generate_api_credentials.py

# 验证私钥
python verify_private_key.py
```

---

## 📈 优化建议

### 1. 调整价差

当前配置: 5%

如果成交率低，可以降到 2-3%：
```python
# run_market_making_safe.py:197
base_spread=Decimal("0.02"),  # 2%
```

### 2. 调整订单大小

当前配置: 1 个 token

如果资金充足，可以增加：
```python
# run_market_making_safe.py:200
order_size=2,  # 2 个 token
```

### 3. 修改订单类型

当前使用: IOC（立即成交或取消）

建议改为: GTC（挂单等待）
```python
# strategies/market_making_strategy.py:206
time_in_force=TimeInForce.GTC,  # 改为 GTC
post_only=True,  # 添加这一行
```

详见 **CODE_REVIEW.md**

---

## 📞 支持

### 文档

- **README.md** - 项目概述
- **CODE_REVIEW.md** - 完整代码审查
- **MARKET_CONFIG_GUIDE.md** - 市场配置详细指南
- **ZEABUR_DEPLOYMENT.md** - Zeabur 部署详细指南

### 工具

- **start.py** - 主启动脚本
- **market_config_manager.py** - 市场配置管理
- **generate_api_credentials.py** - API 凭证生成
- **quick_setup.py** - 快速配置工具

### 常见问题

查看各文档的"常见问题"部分

---

## ✅ 成功标准

部署成功后，您应该：

1. ✅ 策略正常运行 24 小时
2. ✅ 每分钟提交订单
3. ✅ 成交率达到 10-30%
4. ✅ 日盈亏为正或亏损 < 1 USDC
5. ✅ 能够每天更新市场
6. ✅ 能够查看实时日志

---

**最后更新**: 2026-01-29
**版本**: 2.0
**维护**: Martin + Claude Code
